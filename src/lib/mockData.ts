import type { Cliente, Tarefa, Lead, MembroTime } from '../types'

// Mock data cleared — all data is now persisted in Supabase
export const mockClientes: Cliente[] = []
export const mockTarefas: Tarefa[] = []
export const mockLeads: Lead[] = []
export const mockMembros: MembroTime[] = []
